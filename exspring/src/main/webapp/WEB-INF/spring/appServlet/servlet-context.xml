<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<!-- 애노테이션을 이용하여 Spring 웹 애플리케이션을 구현할 때 필용한 객체들을 자동등록 -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<!-- 요청경로에서 /resources/** 어쩌구 저쩌구 한는 이름의 요청이 온다면 location="/resources/ 위치의 파일에서 찾아라-->
	<!-- 지금은 필요없어서 주석처리 -->
	<!-- <resources mapping="/resources/**" location="/resources/" /> -->

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<!-- 스프링(핸들러(컨트롤러))가 뷰 이름을 반환한 경우 -->
	<!-- 뷰 이름의 앞에 "/WEB-INF/views/"를 붙이고 ,뒤에 ".jsp" 를 붙여서 -->
	<!-- 그 경로의 파일로 이동(forward)하도록 설정 -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- "com.exam.exspring"패키지 아래에서 
	@Component,@Controller,@Service,@Repository 등의 애노테이션이 적용된
	클래스들의 객체를 생성하여 스프링(객체 컨테이너)에 자동등록
	 -->
	<context:component-scan base-package="com.exam.exspring" />
	
	<!-- 데이터 베이스 연결을 담당하는 커넥션풀로서 DBCP를 등록 -->
	<beans:bean id="ds" class="org.apache.commons.dbcp2.BasicDataSource">
		<beans:property name="driverClassName" value="oracle.jdbc.OracleDriver"/>
		<beans:property name="url" value="jdbc:oracle:thin:@localhost:1521:xe"/>
		<beans:property name="username" value="web"/>
		<beans:property name="password" value="web01"/>
	</beans:bean>
	
	
	<!-- MyBatis 본체를 등록 (커넥션을 제공하는 객체와 설정파일 위치를 설정 )-->
	<beans:bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
	  <beans:property name="dataSource" ref="ds" />
	 <!--  <beans:property name="configLocation" value="/exspring/src/main/resources/mybatis/mybatis-config.xml" /> -->
	  <beans:property name="configLocation" value="classpath:/mybatis/mybatis-config.xml" />
	</beans:bean>
	
</beans:beans>
